# What gets installed. Re-run ./install.sh after editing; anything removed here gets unlinked.
#   own:<name>                  this library's skills/
#   mattpocock:<bucket>/<name>  vendor/mattpocock/skills/
#
# [auto]   = has a description, fires on its own, costs context every turn
# [manual] = disable-model-invocation, costs nothing until you type its name
#
# superpowers is vendored here (duplicate plugin disabled, one source, no double-loading).
# Nothing left installed from it: brainstorming + verification-before-completion merged into
# own:brainstorming below (one skill for explore-before-start and verify-before-done).
# systematic-debugging cut outright. The rest were already cut: writing-plans, executing-plans,
# test-driven-development, subagent-driven-development, requesting-/receiving-code-review,
# using-git-worktrees, finishing-a-development-branch, writing-skills, using-superpowers,
# dispatching-parallel-agents (dispatching itself lives in working-with-agents).

own:design-craft                    # [auto]   build UI
own:design-review                   # [auto]   judge UI
own:research                        # [auto]   primary sources -> cited .md
own:grilling                        # [auto]   stress-test a plan before building
own:brainstorming                   # [auto]   explore fully before starting; verify before claiming done
own:working-with-agents             # [auto]   dispatch subagents + write their instructions
own:improve-skill                   # [manual] fix a skill that just misbehaved
mattpocock:engineering/wizard       # [auto]   generate a bash wizard for human-only steps
own:wayfinder                       # [auto]   chart work too big for one session



# Merged away
#   grill-me, grill-with-docs                -> own:grilling (2-line wrappers)
#   writing-for-agents, dispatching-parallel-agents -> own:working-with-agents
#   superpowers brainstorming + verification-before-completion -> own:brainstorming
#
# Promoted into CLAUDE.md + AGENTS.md (always on, no longer skills)
#   unslop
#
# Cut outright, not installed anywhere
#   superpowers: systematic-debugging, using-superpowers, writing-plans, executing-plans,
#     subagent-driven-development, test-driven-development, using-git-worktrees,
#     requesting/receiving-code-review, finishing-a-development-branch, writing-skills
#   emil: apple-design/emil-design-eng/animate/animation-vocabulary/pick-ui-library absorbed into
#     design-craft references instead; write-swift, animate-expo, ask-sonner, prototype unused
#   mattpocock: ask-matt unused
