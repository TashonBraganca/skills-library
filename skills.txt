# What gets installed. Re-run ./install.sh after editing; anything removed here gets unlinked.
#   own:<name>                  this library's skills/
#   mattpocock:<bucket>/<name>  vendor/mattpocock/skills/
#
# [auto]   = has a description, fires on its own, costs context every turn
# [manual] = disable-model-invocation, costs nothing until you type its name
#
# superpowers is vendored here (duplicate plugin disabled, one source, no double-loading).
# Trimmed to the three that matter: brainstorming, systematic-debugging (edge cases),
# verification-before-completion (harsh rechecking before claiming done). Cut: writing-plans,
# executing-plans, test-driven-development, subagent-driven-development, requesting-/
# receiving-code-review, using-git-worktrees, finishing-a-development-branch, writing-skills,
# using-superpowers, dispatching-parallel-agents (dispatching itself lives in working-with-agents).

own:design-craft                    # [auto]   build UI
own:design-review                   # [auto]   judge UI
own:research                        # [auto]   primary sources -> cited .md
own:grilling                        # [auto]   stress-test a plan before building
own:working-with-agents             # [auto]   dispatch subagents + write their instructions
own:improve-skill                   # [manual] fix a skill that just misbehaved
mattpocock:engineering/wizard       # [auto]   generate a bash wizard for human-only steps
own:wayfinder                       # [auto]   chart work too big for one session

superpowers:brainstorming           # [auto]
superpowers:systematic-debugging    # [auto]
superpowers:verification-before-completion # [auto]


# Merged away
#   grill-me, grill-with-docs      -> own:grilling (they were 2-line wrappers)
#   writing-for-agents             -> own:working-with-agents
#   dispatching-parallel-agents    -> own:working-with-agents
#
# Promoted into CLAUDE.md + AGENTS.md (always on, no longer skills)
#   unslop
#   verification-before-completion -> your own harder version already lives there
#
# Provided by the superpowers plugin, not by this library
#   brainstorming, systematic-debugging, using-superpowers, writing-plans, executing-plans,
#   subagent-driven-development, test-driven-development, using-git-worktrees,
#   requesting/receiving-code-review, finishing-a-development-branch, writing-skills
#
# Cut: emil apple-design/emil-design-eng/animate/animation-vocabulary/pick-ui-library absorbed into
# design-craft references; write-swift, animate-expo, ask-sonner, prototype, ask-matt unused.
